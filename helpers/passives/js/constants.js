define([

], function (

) {
	return {
		lineWidth: 5,
		blockSize: 20,
		defaultDistance: 50,
		defaultDistanceInc: 60,
		defaultAngle: Math.PI / 2,
		defaultAngleInc: Math.PI / 8,
		gridSize: 30
	};
});
